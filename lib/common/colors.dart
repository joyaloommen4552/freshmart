import 'package:flutter/material.dart';

class AppColors {
  // (Emerald Green)
  static const Color primary = Color(0xFF50C878);

  // (Deep Orange)
  static const Color secondary = Color(0xFFFF8C00);

  // Neutral Colors
  static const Color black = Colors.black;
  static const Color white = Colors.white;
  static const Color grey = Colors.grey;

  // Optional Shades
  static const Color primaryLight = Color(0xFF7FDFA2);
  static const Color secondaryLight = Color(0xFFFFB566);
}
